# ─────────────────────────────────────────────────────────────────────────────
# Sanraksh — Minimal requirements for LOCAL SETUP (judges use this)
# Only the packages actually needed to run the app locally.
#
# Usage:  pip install -r requirements_local.txt
# ─────────────────────────────────────────────────────────────────────────────

# FastAPI + Server
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database (SQLite — no PostgreSQL needed)
sqlalchemy>=2.0.23

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Configuration & Validation
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0

# HTTP
httpx>=0.25.2
requests>=2.31.0
